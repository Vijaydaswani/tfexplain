# Terraform Data tfplan Source

This project is used by `samples/tfplans/generate.sh` to create local binary `.tfplan` samples.

It uses Terraform's built-in `terraform_data` resource, so it does not need cloud credentials.
